/// <summary>
/// REPLACED — use HighlightPlus.HighlightProfile (ScriptableObject) instead.
/// Create profiles in Unity: Create → Highlight Plus → Profile.
/// Assign them as [SerializeField] HighlightProfile on FresnelHighlighter.
///
/// Original presets (create as HighlightProfile assets):
///   ToolPreset       → outline cyan (0.25, 0.85, 1), width 1, no see-through
///   GrabbablePreset  → outline cyan (0.25, 0.85, 1), width 1, no see-through
///   BuildingPreset   → outline cyan, width 1.2 (Phase D)
///   WrenchEnable     → outline green (0.3, 1, 0.3) (Phase D)
///   WrenchDisable    → outline red (1, 0.3, 0.3) (Phase D)
///
/// This file kept as documentation only. Can be deleted.
/// </summary>
public static class HighlightStyle_REPLACED
{
	// see FresnelHighlighter.cs for HighlightProfile [SerializeField] references
}